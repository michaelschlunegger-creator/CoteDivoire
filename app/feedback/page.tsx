import type { Metadata } from "next";
import { PageShell } from "@/components/PageShell";
import { FeedbackForm } from "@/components/FeedbackForm";
import { requireEventUser } from "@/lib/event-auth";
import { participantDisplayName } from "@/lib/participant-name";
export const metadata:Metadata={title:"Feedback"};export const dynamic="force-dynamic";
export default async function FeedbackPage(){const user=await requireEventUser("/feedback");const name=participantDisplayName(user.displayName,user.email);return <PageShell><main><section className="page-hero survey-page-hero"><div className="page-hero-inner"><div className="breadcrumb"><span>Home</span><span>/</span><span>Participant survey</span></div><p className="eyebrow">HELP US RAISE THE STANDARD</p><h1>Your perspective.<br/>Our next benchmark.</h1><p>A confidential 20-question evaluation designed for meaningful technical-event improvement—not generic feedback.</p></div></section><div className="survey-page-shell"><aside className="survey-page-intro"><span className="tag">PRIVATE PARTICIPANT SURVEY</span><h2>Specific evidence creates better events.</h2><p>Evaluate the complete participant journey: registration, programme, technical relevance, speakers, connections, venue, hospitality, travel support and digital experience.</p><div className="survey-meta-grid"><div><strong>20</strong><span>professional questions</span></div><div><strong>≈ 6</strong><span>minutes to complete</span></div><div><strong>100%</strong><span>private response</span></div></div><div className="trust-note"><span>⌾</span><span>Signed in securely as {name}. Every submission remains available in your private participant hub.</span></div></aside><FeedbackForm email={user.email} defaultName={name}/></div></main></PageShell>}
