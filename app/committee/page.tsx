import type {Metadata} from "next";import {PageShell} from "@/components/PageShell";import {COMMITTEE} from "@/lib/event-content";
export const metadata:Metadata={title:"Technical committee"};
export default function CommitteePage(){return <PageShell><main><section className="page-hero"><div className="page-hero-inner"><div className="breadcrumb"><span>Home</span><span>/</span><span>Committee</span></div><p className="eyebrow">TECHNICAL COMMITTEE · 03 JULY 2026 LIST</p><h1>Regional expertise.<br/>Shared direction.</h1><p>The committee roster below is reproduced from the supplied organizer list.</p></div></section><section className="content-shell"><div className="committee-grid">{COMMITTEE.map(([name,role,company])=><article className="committee-card" key={`${name}-${company}`}><span>{role}</span><strong>{name}</strong><small>{company}</small></article>)}</div></section></main></PageShell>}
