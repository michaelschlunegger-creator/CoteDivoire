"use client";import{useMemo,useState}from"react";import{CORE_AGENDA}from"@/lib/event-program";import type{ManagedActivity}from"@/lib/activities";
export function ParticipantPlanner({initialActivityIds,activities}:{initialActivityIds:string[];activities:ManagedActivity[]}){const[reserved,setReserved]=useState(new Set(initialActivityIds));const[day,setDay]=useState(1);const[busy,setBusy]=useState<string|null>(null);const[message,setMessage]=useState("");const agenda=useMemo(()=>[...(CORE_AGENDA[day]||[]),...activities.filter(a=>a.day===day&&reserved.has(a.id)).map(a=>({time:a.time,title:a.title,location:a.location,kind:"Your reservation"}))].sort((a,b)=>a.time.localeCompare(b.time)),[day,reserved,activities]);async function toggle(activityId:string){const active=reserved.has(activityId);setBusy(activityId);setMessage("");try{const response=await fetch("/api/me/activities",{method:active?"DELETE":"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({activityId})});const data=await response.json() as{error?:string};if(!response.ok)throw new Error(data.error||"Unable to update your reservation.");setReserved(current=>{const next=new Set(current);if(active)next.delete(activityId);else next.add(activityId);return next});setMessage(active?"Reservation removed from your agenda.":"Reserved and added to your personal agenda.")}catch(error){setMessage(error instanceof Error?error.message:"Unable to update your reservation.")}finally{setBusy(null)}}return <section className="participant-planner"><div className="planner-intro"><div><span className="tag">MY EVENT JOURNEY</span><h2>Build your experience.</h2><p>Reserve official dinners, excursions, receptions and networking activities. Every choice updates your private agenda immediately.</p></div><span className="planner-counter"><strong>{reserved.size}</strong><small>personal reservations</small></span></div><div className="planner-layout"><div className="activity-market"><div className="planner-section-head"><div><span>OPTIONAL EXPERIENCES</span><h3>Choose what joins your journey</h3></div></div><div className="activity-grid">{activities.map(activity=>{const active=reserved.has(activity.id);return <article className={`activity-card ${active?"reserved":""}`} key={activity.id}><div className="activity-top"><span>{activity.type}</span><small>DAY {activity.day}</small></div><h4>{activity.title}</h4><p>{activity.description}</p><div className="activity-details"><span><b>{activity.time}</b>–{activity.endTime}</span><span>{activity.location}</span><span>{activity.capacity} places</span>{activity.isPlaceholder&&<span>Details awaiting organizer approval</span>}</div><button disabled={busy===activity.id||(!activity.isOpen&&!active)} onClick={()=>void toggle(activity.id)} className={active?"reservation-button active":"reservation-button"}>{busy===activity.id?"Updating…":active?"✓ Reserved · Remove":activity.isOpen?"Reserve my place →":"Reservations not open"}</button></article>})}</div>{message&&<p className="planner-message">{message}</p>}</div><aside className="personal-agenda"><div className="agenda-label"><span>LIVE PERSONAL VIEW</span><i/></div><h3>Your day, at a glance.</h3><div className="agenda-tabs">{[1,2,3].map(number=><button className={day===number?"active":""} onClick={()=>setDay(number)} key={number}><small>DAY</small><strong>0{number}</strong></button>)}</div><div className="agenda-date">{day===1?"19 APRIL 2027":day===2?"20 APRIL 2027":"21 APRIL 2027"}</div><div className="agenda-stream">{agenda.map(item=><div className={`agenda-entry ${item.kind==="Your reservation"?"personal":""}`} key={`${item.time}-${item.title}`}><time>{item.time}</time><i/><div><small>{item.kind}</small><strong>{item.title}</strong><span>{item.location}</span></div>{item.kind==="Your reservation"&&<b>YOURS</b>}</div>)}</div></aside></div></section>}
